/**
 * Business-specific details referenced across the legal pages (/terms,
 * /privacy, /refund-policy) and /contact. Every value here is a placeholder —
 * replace them with Lootza's real information before going live. Each is also
 * rendered with a visible highlight (see components/legal/Fill.tsx) so an
 * unfilled placeholder is obvious on the live site, not just in source.
 */
export const LEGAL_BUSINESS_NAME = "Lootza";
export const LEGAL_JURISDICTION = "the State of Florida, USA";
export const SUPPORT_EMAIL = "bilode11.71@gmail.com";
export const LEGAL_LAST_UPDATED = "September 10, 2026";
